# 🚀 Быстрый запуск лендинга "Speechka"

## 📋 Что нужно сделать перед запуском:

### 1. Замените ссылки на бота
В файле `index.html` найдите и замените все вхождения `@YourBot` на реальный username вашего Telegram-бота:

```html
<!-- Замените на ваш реальный username -->
<a href="https://t.me/Speeechka_bot" class="cta-button" target="_blank">
```

### 2. Обновите домен
В файлах `sitemap.xml` и `robots.txt` замените `govorilegko.ru` на ваш реальный домен.

## 🎯 Запуск локально:

### Вариант 1: Python (рекомендуется)
```bash
python -m http.server 8000
```
Откройте: http://localhost:8000

### Вариант 2: Node.js
```bash
npx live-server --port=8000
```
Откройте: http://localhost:8000

### Вариант 3: Просто откройте файл
Дважды кликните на `index.html` в браузере

## 🌐 Размещение на хостинге:

1. **Загрузите все файлы** на ваш веб-сервер
2. **Убедитесь, что `.htaccess` работает** (для Apache)
3. **Проверьте SSL-сертификат** (HTTPS обязателен)

## ✅ Проверка готовности:

- [ ] Ссылки на бота обновлены
- [ ] Домен обновлен в sitemap.xml
- [ ] Лендинг открывается без ошибок
- [ ] Все кнопки ведут на правильные ссылки
- [ ] Адаптивность работает на мобильных

## 🎨 Кастомизация:

### Цвета
Отредактируйте CSS-переменные в `styles.css`:
```css
:root {
    --primary-blue: #E3F2FD;
    --primary-mint: #E8F5E8;
    --accent-coral: #FF6B6B;
    --accent-orange: #FF8E53;
}
```

### Тексты
Все тексты находятся в `index.html` - просто отредактируйте содержимое.

## 📞 Поддержка:
- Email: support@speechka.ru
- Telegram: @Speeechka_bot

---

**Готово! Ваш лендинг работает! 🎉**
